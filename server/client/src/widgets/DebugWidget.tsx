import { FC } from 'react';
import WidgetWrapper from 'components/WidgetWrapper';

const DebugWidget: FC = () => {
  return (
    <WidgetWrapper>
      <p>Things to be debugged...</p>
    </WidgetWrapper>
  );
};

export default DebugWidget;
